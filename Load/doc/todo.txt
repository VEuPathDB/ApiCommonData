fix NRDB plugin.  it should take a file that has db-symbol, db-name, db-release.  the plugin should check all of them before running, and then query for the external_db_release_id

DONE (a long time ago) improve Sftp module by using UNIX sftp instead of Perl


